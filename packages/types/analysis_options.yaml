# This file configures the analyzer for the types package
include: package:lints/recommended.yaml

linter:
  rules:
    - prefer_const_constructors
    - prefer_final_fields
    - unnecessary_null_in_if_null_operators
    - prefer_single_quotes
