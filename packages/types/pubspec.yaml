name: shsp_types
description: Type definitions for Single HandShake Protocol
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.8.0

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.10.2
  json_serializable: ^6.11.1
  lints: ^3.0.0
  test: ^1.24.0
