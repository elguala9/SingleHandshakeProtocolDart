import 'dart:io';

/// Wrapper interface for RawDatagramSocket
/// Provides a concrete implementation of RawDatagramSocket with additional features
abstract interface class IRawShspSocket implements RawDatagramSocket {
  // Implements all RawDatagramSocket methods and properties
}
