name: shsp_tests
description: Tests for Single HandShake Protocol
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: '>=3.9.4 <4.0.0'

dependencies:
  shsp:
    path: ../shsp
  singleton_manager: ^0.3.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
