name: shsp_interfaces
description: Abstract interfaces and contracts for Single HandShake Protocol (SHSP) implementations - defines the core API for peer-to-peer networking.
version: 1.0.0
homepage: https://github.com/lgualandi/SingleHandShakeProtocolDart
repository: https://github.com/lgualandi/SingleHandShakeProtocolDart
issue_tracker: https://github.com/lgualandi/SingleHandShakeProtocolDart/issues
resolution: workspace
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  json_annotation: ^4.9.0
  shsp_types:
    path: ../types

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
