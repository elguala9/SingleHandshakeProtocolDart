name: shsp_interfaces
description: Interfaces for Single HandShake Protocol
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  json_annotation: ^4.9.0
  shsp_types:
    path: ../types

dev_dependencies:
  build_runner: ^2.10.2
  json_serializable: ^6.11.1
  lints: ^3.0.0
  test: ^1.24.0
